#include "monty.h"
/**
 * exec_nop - execute nop opcode
 * @top: pointer to the top of stack
 * @line_number: number of line in the file
 * Return: void
 */
void exec_nop(__attribute__((unused)) stack_t **top, unsigned int line_number)
{
	line_number += 0;
}
